# PostgreSQL（Docker Compose 或本地后端）
POSTGRES_USER=mediation
POSTGRES_PASSWORD=mediation_secret
POSTGRES_DB=mediation

# 后端连接串（本地跑 server 时使用）
DATABASE_URL=postgresql://mediation:mediation_secret@localhost:5432/mediation

# 大模型代理（至少配置一个，前端可选千问或 DeepSeek）
QWEN_API_KEY=
DEEPSEEK_API_KEY=

# 可选：自定义 API 地址（不填则用官方默认）
# QWEN_API_BASE=https://dashscope.aliyuncs.com/compatible-mode/v1
# DEEPSEEK_API_BASE=https://api.deepseek.com/v1

# 鉴权（后端）
JWT_SECRET=your-jwt-secret-change-in-prod
# 首次启动无用户时创建默认管理员（可选）
# ADMIN_DEFAULT_PASSWORD=admin123
